server:
    port: 8080
    servlet:
        context-path: /api
spring:
    application:
        name: gateway
    r2dbc:
        url: ${DB_URL}
        username: ${DB_USERNAME}
        password: ${DB_PASSWORD}